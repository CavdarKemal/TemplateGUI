Folgendes habe ich geändert: Package wieder auf de.cavdar refactored.

Folgendes bitte ändern:
1.  die Konstanten NEW_CONNECTION, LOADING_NODE, ... in den View-Klassen bitte in eine eigene "AppConstants.java§ Klasse auslagern und als publich static declarieren, damit sie von allen View-Klassen verwendet werden können.
1.  die GUI soll als Konfig nur die Datien mit Namen-Pattern "*-config.properties" auflisten bzw. laden
2.  DB-Viewer: der Combobox für vorhandene DB-Verbindungen zeigt unterschiedliche DB-Vebindungen, je nachdem welche Config (ene-config.properties oder gee-config.properties) selektiertt ist
3.  Beim Wechseln der Config wird die DB-Verbindung, die man zugewiesen hatte, nicht gespeichert
4.  ITSQ-Explorer: Beim load-Button wird auf das target-Verzeichnis gewechselt.
