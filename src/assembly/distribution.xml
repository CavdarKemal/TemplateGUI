<assembly xmlns="http://maven.apache.org/ASSEMBLY/2.2.0"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/ASSEMBLY/2.2.0
                              http://maven.apache.org/xsd/assembly-2.2.0.xsd">

    <id>distribution</id>

    <formats>
        <format>zip</format>
    </formats>

    <includeBaseDirectory>true</includeBaseDirectory>

    <fileSets>
        <!-- Delivery-Dateien aus Projektwurzel -->
        <fileSet>
            <directory>${project.basedir}/delivery</directory>
            <includes>
                <include>log4j.properties</include>
                <include>*-config.properties</include>
                <include>startGUI.cmd</include>
                <include>startGUI.sh</include>
            </includes>
            <outputDirectory>/</outputDirectory>
        </fileSet>

        <!-- LOCAL Test-Set -->
        <fileSet>
            <directory>${project.basedir}/delivery/LOCAL</directory>
            <outputDirectory>/LOCAL</outputDirectory>
            <includes>
                <include>**/*</include>
            </includes>
        </fileSet>

        <!-- Entpackte Testfaelle aus Projektverzeichnis einbinden -->
        <fileSet>
            <directory>${project.basedir}/ITSQ</directory>
            <outputDirectory>/ITSQ</outputDirectory>
            <includes>
                <include>**/*</include>
            </includes>
        </fileSet>

        <!-- Dependencies in lib-Verzeichnis -->
        <fileSet>
            <directory>${project.build.directory}/lib</directory>
            <outputDirectory>/lib</outputDirectory>
            <includes>
                <include>*.jar</include>
            </includes>
        </fileSet>
    </fileSets>

    <!-- Projekt-JAR einbinden -->
    <files>
        <file>
            <source>${project.build.directory}/${project.build.finalName}.jar</source>
            <outputDirectory>/lib</outputDirectory>
        </file>
    </files>

</assembly>
